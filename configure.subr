#!/bin/sh

CONFIGURE_MK=configure.mk.tmp

echo "# generated by ./configure" > ${CONFIGURE_MK}

update_configure_mk() {
    if diff configure.mk "${CONFIGURE_MK}"; then
        rm "${CONFIGURE_MK}"
    else
        mv "${CONFIGURE_MK}" configure.mk
    fi
}
